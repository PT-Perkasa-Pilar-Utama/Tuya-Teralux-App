# Codex Instructions

Use `.agents/README.md` as the canonical starting point.

Then load and follow, in order:

1. `.agents/project-context.md`
2. `.agents/coding-rules.md`
3. `.agents/workflow-rules.md`

If a direct user request conflicts with repository rules, prioritize the direct user request unless it creates a safety or security issue.
