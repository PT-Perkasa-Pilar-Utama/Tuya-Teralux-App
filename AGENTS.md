# Codex Instructions

All repository AI rules are centralized in the `.agents/` directory.

Always read and apply the instructions in `.agents/` before proceeding with any task. Bridge files must not define parallel or conflicting rules.

If a direct user request conflicts with repository rules, prioritize the direct user request unless it creates a safety or security issue.
