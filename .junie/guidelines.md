# Junie Guidelines

This repository centralizes all AI instructions in `.agents/`.

Always read and apply these files in order:

1. `.agents/README.md`
2. `.agents/project-context.md`
3. `.agents/coding-rules.md`
4. `.agents/workflow-rules.md`

Task-specific:
- For API contract requests, also read `.agents/api-contract-guide.md`.

Policy:
- Treat `.agents/` as the authoritative source.
- Do not create parallel rule sets outside `.agents/`.
