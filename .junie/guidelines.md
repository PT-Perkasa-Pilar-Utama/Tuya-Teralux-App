# Junie Guidelines

This repository centralizes all AI instructions in the `.agents/` directory.

Always read and follow the instructions in `.agents/` before starting any task. Treat `.agents/` as the authoritative source of truth. Bridge files must not define parallel or conflicting rules.
