# GitHub Copilot Instructions

All repository AI rules are centralized in the `.agents/` directory.

Always read and apply the instructions in `.agents/` before generating suggestions. Maintain consistency with the architecture and workflow defined therein.
