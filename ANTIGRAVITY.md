# Antigravity Instructions

All project AI rules are centralized in the `.agents/` directory.

Follow all instructions in `.agents/` carefully. Do not invent parallel rule sets outside `.agents/`. Bridge files must not define parallel or conflicting rules.
