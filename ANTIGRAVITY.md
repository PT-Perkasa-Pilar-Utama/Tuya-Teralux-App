# Antigravity Instructions

All project AI rules are centralized in `.agents/`.

Follow these files in sequence:

1. `.agents/README.md`
2. `.agents/project-context.md`
3. `.agents/coding-rules.md`
4. `.agents/workflow-rules.md`

Do not invent parallel rule sets outside `.agents/`. If additional rules are needed, add them under `.agents/` and reference them from `.agents/README.md`.
