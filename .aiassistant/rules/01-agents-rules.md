---
apply: always
---

# Repository Rules Source

Use `.agents/README.md` as the canonical entrypoint for repository rules.

Read and apply, in order:

1. `.agents/project-context.md`
2. `.agents/coding-rules.md`
3. `.agents/workflow-rules.md`

Task-specific:
- For API contract requests, read `.agents/api-contract-guide.md`.

Keep all future AI behavior updates centralized under `.agents/`.
